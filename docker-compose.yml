version: "3"

services:
  db:
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    image: postgres:10.6-alpine
    ports:
      - 5432:5432
  app:
    build: .
    env_file:
      - .env
    command: sh -c "yarn migration:build && yarn start"
    expose:
      - 3000
    ports:
      - 3000:3000
    depends_on:
      - db
    volumes:
      - .:/usr/app
